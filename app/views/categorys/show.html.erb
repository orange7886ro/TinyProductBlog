<div class="col-md-12">
  <div class="group">
    <%= link_to("Edit", edit_tag_path(@category), class: "btn btn-primary pull-right")%>
    <%= link_to("New tagging", new_tag_tagging_path(@category), class: "btn btn-primary pull-right")%>
    <%= link_to("Back", tags_path , class: "btn btn-primary pull-right")%>
  </div>
  <h2><%= @category.name%></h2>
  <table class="table table-hover">
    <thead>
    <tr>
      <td>image</td>
      <td>Product name</td>
      <td>Do you like it?(VOTE)</td>
      <td>vote results</td>
      <td>description</td>
      <td>Posts</td>
    </tr>
    </thead>
    <tbody>
    <% @product.each do |product| %>
        <tr>
          <td><%= image_tag product.image_url(:product_thumb).to_s %></td>
          <td><%= link_to(product.title , product_path(product)) %></td>
          <td><%= link_to("good", vote_up_product_path(product), :method => :post) %> OR
            <%= link_to("bad", vote_down_product_path(product), :method => :post) %></td>
          <td> good:<%= product.votes_for %>,bad: <%= product.votes_against %></td>
          <td><%= product.description %></td>
          <td><%= link_to("posts" , product_posts_path(product)) %></td>
          <td>
            <%= link_to("Edit", edit_product_path(product), class: "btn btn-sm btn-default")%>
            <%= link_to("Delete", product_path(product), class: "btn btn-sm btn-default", method: :delete, data: { confirm: "Are you sure?" } )%>
          </td>
        </tr>
    <% end %>
    </tbody>
  </table>

</div>