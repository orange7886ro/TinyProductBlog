<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <a class="navbar-brand" href="/">Blog Hunt</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
      <% Product.category_list.each do |category| %>
        <li class="active">
          <%= link_to(category.name,  tag_products_path(category)) %>
        </li>
      <% end %>

      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if !current_user %>
        <li> <%= link_to("登入", new_user_session_path) %> </li>
        <% else %>

        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">
            Hi!, <%= current_user.email %>
            <%= current_user.role.name unless current_user.role.nil? %>
            <b class="caret"></b>
          </a>
          <ul class="dropdown-menu">
            <li>
              <%= link_to("登出", destroy_user_session_path, :method => :delete ) %>
            </li>
            <li>
              <%= link_to("My Collection", collections_path(current_user) ) %>
            </li>
            <li>
              <%= link_to("編輯Profile", profile_path(current_user) ) %>
            </li>
            <li>
              <%= link_to("編輯Category", tags_path ) %>
            </li>

          </ul>
        </li>


        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>